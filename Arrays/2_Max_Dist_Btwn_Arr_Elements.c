#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <unordered_set.h>

//Given an array, find the largest distance between two array elements
//such that arr[j] > arr[i]
int maxDistBtwnArrElements(int arr[], int len)
{
	int max = 0;

	arr
	return max;
}

int main(void)
{
	return 0;
}

